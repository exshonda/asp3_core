# prcv_dtq — データキュー受信（ポーリング）

## 種別
〔T〕タスクコンテキスト専用（非タスクから呼ぶと `E_CTX`）

## C言語API
```c
ER ercd = prcv_dtq(ID dtqid, intptr_t *p_data);
```

## パラメータ
| 名称 | 型 | 説明 |
|---|---|---|
| `dtqid` | `ID` | 対象データキューのID番号 |
| `p_data` | `intptr_t *` | 受信データの格納先 |

## リターンパラメータ
| 名称 | 型 | 説明 |
|---|---|---|
| `ercd` | `ER` | 正常終了（`E_OK`）またはエラーコード |
| `*p_data` | `intptr_t` | 受信したデータ |

## エラーコード
| コード | 条件 |
|---|---|
| `E_OK` | 正常終了（データを受信した） |
| `E_CTX` | 非タスクコンテキスト／CPUロック状態 |
| `E_ID` | `dtqid` が不正・範囲外 |
| `E_TMOUT` | キューが空でデータを受信できなかった（ポーリング失敗） |

## 機能
`dtqid` で指定したデータキューから先頭の1件を取り出し、`*p_data` に格納する。
`rcv_dtq` のポーリング版であり、待ち状態に遷移しない。

キューにデータが存在する場合は即座にリターンする。データを取り出したことで送信待ちタスクが
いる場合は、そのタスクが送信したデータをキューに追加してから待ち解除する。

キューが空で送信待ちタスクもいない場合は、待たずに `E_TMOUT` を返す。

## 使用例
```c
void consumer_task(intptr_t exinf) {
    intptr_t data;
    while (true) {
        ER ercd = prcv_dtq(MSG_DTQ, &data);
        if (ercd == E_OK) {
            process((Message *)data);
        } else {
            do_other_work();   /* データが無ければ別処理 */
        }
    }
}
```

## 関連
- `rcv_dtq` — データキュー受信（ブロッキング）
- `trcv_dtq` — 受信（タイムアウト付き）
- `snd_dtq` — データキュー送信（ブロッキング）
- 仕様書 4.4.3 データキュー機能
